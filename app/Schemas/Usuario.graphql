type Query {
  usuarios: [Usuario]
  usuario(
    id: ID!
  ): Usuario
}

type Mutation {
  deletarUsuario(
    id: ID!
  ): Usuario
}

type Usuario {
  id: ID!
  username: String!
  email: String!
  password: String!
  tipo: String!
}